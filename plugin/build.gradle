version = rootProject.version

apply plugin: 'com.github.johnrengelman.shadow'
apply from: 'vars.gradle'
apply from: 'jar.gradle'

dependencies {
    implementation project(':plugin-api')

    implementation 'org.comroid:upd8r:0.1.3'
}
